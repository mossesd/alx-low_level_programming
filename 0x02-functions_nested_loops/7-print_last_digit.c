#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @i: the number
 * Return: always returns succes
 */
int print_last_digit(int i)
{
	int last_digit;
	last_digit = i % 10;

	return (last_digit);
}
