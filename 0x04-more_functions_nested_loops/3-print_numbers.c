#include "main.h"

/**
 * print_numbers - prints numbers with a new line
 * Return: Always succes
 */

void print_numbers(void)
{
	for (int i = 48; i <= 57; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
