#include "main.h"

/**
 * mul - returns the product of two integers
 * @a: the number to be multipled
 * @b: the number to be multiplied
 * Return: Always return succes
 */

int mul(int a, int b)
{
	return (a * b);
}
